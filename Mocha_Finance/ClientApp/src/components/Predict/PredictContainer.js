import React, { Component } from "react";
import PredictPresenter from "./PredictPresenter";

class PredictContainer extends Component {
  render() {
    return <PredictPresenter />;
  }
}

export default PredictContainer;
