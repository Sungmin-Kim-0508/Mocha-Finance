import React, { Component } from "react";
import NavMenuPresenter from "./NavMenuPresenter";

class NavMenuContainer extends Component {
  render() {
    return <NavMenuPresenter />;
  }
}

export default NavMenuContainer;
