const HOME = "/";
const FAVORITE = "/favorite";
const PREDICT = "/predict";
const SEARCH = "/search";
const LOGIN = "/login";
const REGISTER = "/register";

const routes = {
  home: HOME,
  favorite: FAVORITE,
  predict: PREDICT,
  search: SEARCH,
  login: LOGIN,
  register: REGISTER
};

export default routes;
